services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: musicstore
      POSTGRES_PASSWORD: musicstore
      POSTGRES_USER: musicstore
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
