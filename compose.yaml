services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: musicstore
      POSTGRES_PASSWORD: musicstore
      POSTGRES_USER: musicstore
    ports:
      - '5432:5432'
    volumes:
      - musicstore-postgres:/var/lib/postgresql/data

volumes:
  musicstore-postgres:
